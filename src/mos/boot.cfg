MEMORY {
    ZP:             start = $0070, size = $0020, type = rw, define = yes;
    MOS_FONT:       start = $C000, size = $0300, file = %O, fill=yes, fillval=$FF;
    MOS:            start = $C300, size = $3AFA, file = %O, fill=yes, fillval=$FF;
    MOS_VECS:       start = $FFFA, size = $06, file = %O, fill=yes, fillval=$FF;
}
SEGMENTS {
    CODE:     	        load = MOS, type = ro;
    BOOT_VECS:          load = MOS_VECS, type = ro;
    FONT:               load = MOS_FONT, type = ro;

}
FEATURES {
}
SYMBOLS {
}